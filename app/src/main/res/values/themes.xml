<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="TCMAppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/tcm_color_primary</item>
        <item name="colorPrimaryDark">@color/tcm_color_primary_dark</item>
        <item name="colorAccent">@color/tcm_color_accent</item>
        <item name="android:windowBackground">@null</item>
        <item name="actionBarSize">40dp</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>